
// Create the diagram
var diagram_instance = new dipam_diagram(  JSON.parse(decode_json(workflow))  );

// Create the interface
var interface_instance = new dipam_interface();

// Create the unit view
var dipam_unit_value = new dipam_unit_view();
